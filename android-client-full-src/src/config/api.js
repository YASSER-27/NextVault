import { Platform } from 'react-native';

// Standard pattern for resolving localhost/local IP dynamically. 
// For physical devices, you must change this to your PC's IP address on the local network.
// Example: export const BASE_URL = 'http://192.168.1.5:3000';
export const BASE_URL = 'http://192.168.1.62:3000';
